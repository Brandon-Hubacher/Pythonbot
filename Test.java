import org.w3c.dom.ls.LSOutput;

import java.awt.*;
import java.sql.SQLOutput;
import java.util.Arrays;

public class Test {
    public static void main(String[] args) {
        IDAndName INInst = new IDAndName();
        /*
        int[] rank = new int[] {10, 80, 30, 90, 40, 50, 70};
        int pivot = rank[0];
        int negDif = 0;
        int posDif = 0;
        int t = 0;
        while(t != rank.length)
        {
            if(rank[t] < rank[t+1])
            {
                negDif = rank[t] - rank[t+1];
                //pivot = rank[t+1];
                t++;
            }
            else if(rank[t] > rank[t+1])
            {
                int temp = rank[t];
                rank[t] = rank[t+1];
                rank[t+1] = temp;
                if(posDif >= (rank[t+1] - rank[t] + negDif))
                {
                    int temp2 = rank[t-1];
                    rank[t-1] = rank[t];
                    rank[t] = temp2;
                }
                else if(posDif < (rank[t+1] - rank[t] + negDif))
                {
                    int temp3 = rank[t-2];
                    int temp4 = rank[t-1];
                    rank[t-2] = rank[t];
                    rank[t-1] = temp3;
                    rank[t] = temp4;

                }
                posDif = rank[t+1] - rank[t];
                t++;
            }
            else if(rank[t] == rank[t+1])
            {
                continue;
            }
        }
        System.out.println(rank);

         */
    }
}
